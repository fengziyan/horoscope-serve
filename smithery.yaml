name: horoscope-server
description: 星座运势服务

build:
  type: docker

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |
    config => ({ command: 'node', args: ['dist/index.js'] }) 